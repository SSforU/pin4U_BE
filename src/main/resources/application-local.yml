server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pin4u_be
    username: pin4u
    password: "lsy12052781!"
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jackson:
    property-naming-strategy: SNAKE_CASE

app:
  search:
    stationRadiusM: 800
    topN: 10
  kakao:
    enabled: true
    api:
      base-url: https://dapi.kakao.com
      # 환경변수 없을 때 바로 죽게 하려면 콜론 기본값 제거:
      key: ${KAKAO_REST_API_KEY}  # ← 환경변수만 허용(기본값 없음)

logging:
  level:
    io.github.ssforu.pin4u: DEBUG
    org.springframework.web: INFO
